language: java

cache:
  directories:
  - $HOME/.m2

addons:
  sonarcloud:
    organization: "grillbaer"

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=grillbaer_persistentds
